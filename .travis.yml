language: ruby
sudo: false
git:
  depth: 1
branch:
  only:
    - master
before_install:
- travis_retry gem install jekyll
- travis_retry gem install html-proofer
- travis_retry gem install kramdown-parser-gfm
rvm:
- 2.7.4
script: jekyll build --safe && htmlproofer --allow_hash_href --only_4xx --file_ignore /blog/201[0-5]/ ./_site
notifications:
  irc:
    on_success: change
    on_failure: change
    channels:
      - irc.libera.chat#georchestra
    template:
      - "%{repository}#%{build_number} (%{branch}) %{message} %{build_url}"
env:
  global:
  - NOKOGIRI_USE_SYSTEM_LIBRARIES=true
